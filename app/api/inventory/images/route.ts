import { NextRequest, NextResponse } from "next/server";
import { requireAdmin } from "@/app/api/leads/admin";
export async function POST(request: NextRequest) { const denied = await requireAdmin(request); if (denied) return denied; const url = (process.env.SUPABASE_URL || process.env.NEXT_PUBLIC_SUPABASE_URL || "").replace(/\/$/, ""); const key = process.env.SUPABASE_SERVICE_ROLE_KEY || ""; if (!url || !key) return NextResponse.json({ error: "Storage is not configured" }, { status: 503 }); const file = (await request.formData()).get("file"); if (!(file instanceof File)) return NextResponse.json({ error: "Select an image or video file" }, { status: 400 }); const isImage = file.type.startsWith("image/"); const isVideo = file.type.startsWith("video/"); const maxSize = isVideo ? 50 * 1024 * 1024 : 10 * 1024 * 1024; if ((!isImage && !isVideo) || file.size > maxSize) return NextResponse.json({ error: isVideo ? "Use a video under 50 MB" : "Use an image under 10 MB" }, { status: 400 }); const path = `kapil/${Date.now()}-${file.name.replace(/[^a-zA-Z0-9._-]/g, "-")}`; const response = await fetch(`${url}/storage/v1/object/chariot-inventory/${path}`, { method: "POST", headers: { apikey: key, Authorization: `Bearer ${key}`, "Content-Type": file.type, "x-upsert": "false" }, body: await file.arrayBuffer() }); if (!response.ok) { const detail = await response.json().catch(() => ({})); return NextResponse.json({ error: detail.message || detail.error || `Storage upload failed (${response.status})` }, { status: 502 }); } return NextResponse.json({ path, media_type: isVideo ? "video" : "image", public_url: `${url}/storage/v1/object/public/chariot-inventory/${path}` }, { status: 201 }); }
